{{> header subject="Advisor Portal updated - New features" is_advisor=true}}

<h1>Platform update deployed</h1>

<p class="lead-text">We shipped new improvements for advisors.</p>

<ul>
  {{#each release_highlights}}
  <li>{{this}}</li>
  {{/each}}
</ul>

<div class="button-center">
  <a href="{{changelog_url}}" class="button">Explore Updates</a>
</div>

{{> footer user_email=user_email is_critical=false current_year=current_year portal_url=portal_url support_url=support_url}}
