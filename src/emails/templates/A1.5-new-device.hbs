{{> header subject="New device login detected" is_advisor=true}}

<h1>New device detected</h1>

<p class="lead-text">We noticed a login to your advisor account from a new device.</p>

<div class="card">
  <div class="card-title">Login details</div>
  <div class="card-content">
    <p><strong>Device:</strong> {{device_name}}</p>
    <p><strong>Location:</strong> {{location}}</p>
    <p><strong>Time:</strong> {{timestamp}}</p>
  </div>
</div>

<div class="button-center">
  <a href="{{review_devices_url}}" class="button">Review Devices</a>
  <a href="{{secure_account_url}}" class="button button-secondary">Secure Account</a>
</div>

<p class="small-text">If this was you, no action needed.</p>

{{> footer user_email=user_email is_critical=true current_year=current_year portal_url=portal_url support_url=support_url}}
