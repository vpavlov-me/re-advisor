      </div>
      
      <!-- Footer -->
      <div class="email-footer">
        <p class="footer-text">
          This email was sent to <strong>{{user_email}}</strong>
        </p>
        
        <div class="footer-links">
          <a href="{{#if portal_url}}{{portal_url}}{{else}}https://advisor.reluna.family{{/if}}" class="footer-link">Open advisor.reluna.family</a>
          <span style="color: #CCCCCC;">•</span>
          <a href="{{#if portal_url}}{{portal_url}}{{else}}https://advisor.reluna.family{{/if}}/settings/notifications" class="footer-link">Notification preferences</a>
          <span style="color: #CCCCCC;">•</span>
          <a href="{{#if support_url}}{{support_url}}{{else}}https://advisor.reluna.family/support{{/if}}" class="footer-link">Support</a>
        </div>
        
        <p class="footer-text" style="margin-top: 24px;">
          <strong>RE:Advisor</strong> — Advisor Platform
        </p>
        
        <p class="footer-text">
          © {{current_year}} RE:Advisor. All rights reserved.
        </p>
        
        {{#unless is_critical}}
        <p class="footer-text" style="margin-top: 20px; font-size: 12px; color: #999999;">
          You're receiving this email because of your activity in the Advisor Portal.<br>
          <a href="{{#if portal_url}}{{portal_url}}{{else}}https://advisor.reluna.family{{/if}}/settings/notifications" style="color: #5B68F4;">Manage email preferences</a> or 
          <a href="{{unsubscribe_url}}" style="color: #999999;">unsubscribe</a>
        </p>
        {{/unless}}
      </div>
    </div>
  </div>
</body>
</html>
