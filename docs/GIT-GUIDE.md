# 📚 Git для начинающих - Family Governance Platform

**Краткий гайд для non-tech команды: как работать с версиями документов**

## 🎯 Что такое Git и зачем он нужен?

Git - это система контроля версий. Представьте Google Docs с историей изменений, но для всего проекта.

**Зачем использовать:**
- 📝 **История изменений** - кто, когда и что изменил
- 🔄 **Совместная работа** - несколько человек редактируют документы
- 💾 **Резервные копии** - ничего не потеряется
- 🚀 **Синхронизация** - изменения доступны всей команде

## 🪟 Для Windows пользователей

**ВАЖНО:** На Windows рекомендуется использовать **Git Bash** вместо PowerShell/CMD для Git команд.

### Как открыть Git Bash:
1. **В папке:** Правой кнопкой → "Git Bash Here"
2. **В VS Code:** Откройте терминал (`Ctrl + ~`) → выберите "Git Bash" в выпадающем меню

### Настройка Git Bash в VS Code:
1. Откройте Settings (`Ctrl + ,`)
2. Найдите: `terminal.integrated.defaultProfile.windows`
3. Установите: `Git Bash`

**Все команды ниже работают одинаково в Git Bash, Mac Terminal, и Linux Terminal.**

---

## ⚡ Основные команды (5 команд = 90% работы)

### 1. Проверить статус изменений
```bash
git status
```
**Что показывает:** какие файлы изменены, добавлены или удалены

**Windows пользователи:** Используйте Git Bash для этой команды!

### 2. Добавить изменения для сохранения
```bash
git add .                    # Добавить все изменения
git add имя-файла.md         # Добавить конкретный файл
```
**Что делает:** помечает файлы для сохранения в истории

### 3. Сохранить изменения с описанием
```bash
git commit -m "Описание что изменили"
```
**Примеры описаний:**
- `"Добавил требования для EPIC-003"`
- `"Обновил статус спринта"`
- `"Исправил опечатки в документации"`

### 4. Отправить изменения в общий репозиторий
```bash
git push
```
**Что делает:** ваши изменения становятся доступны всей команде

### 5. Получить изменения от команды
```bash
git pull
```
**Что делает:** скачивает последние изменения от коллег

## 🔄 Типичный рабочий процесс

```bash
# 1. Получаем последние изменения
git pull

# 2. Редактируем документы в VS Code
# (создаем файлы, изменяем текст)

# 3. Проверяем что изменили
git status

# 4. Добавляем изменения
git add .

# 5. Сохраняем с описанием
git commit -m "Описание изменений"

# 6. Отправляем команде
git push
```

## 📝 Хорошие практики

### Как писать описания коммитов
✅ **Хорошо:**
- `"Добавил пользовательские истории для регистрации советников"`
- `"Обновил спринт-план: завершили задачи 001-003"`
- `"Исправил ошибки в требованиях после ревью"`

❌ **Плохо:**
- `"Изменения"`
- `"Фикс"`
- `"Обновление"`

### Когда делать коммиты
- 📝 **После завершения логической части работы** (например, написали одну user story)
- 🕐 **В конце рабочего дня** (чтобы не потерять работу)
- 🎯 **Перед важными встречами** (чтобы коллеги видели ваш прогресс)

## 🆘 Что делать если что-то пошло не так

### Для Windows: Ошибки с line endings (CRLF vs LF)
```bash
# Если видите предупреждения о CRLF/LF - это нормально для Windows
# Git автоматически конвертирует форматы
# Чтобы настроить один раз:
git config --global core.autocrlf true
```
**Примечание:** Windows использует CRLF для переноса строк, Mac/Linux используют LF. Git автоматически конвертирует их при коммитах.

### Ошибка: "conflict" при git pull
```bash
# 1. Откройте файл с конфликтом в VS Code
# 2. Найдите строки с <<<<<<< и >>>>>>>
# 3. Выберите нужную версию, удалите лишнее
# 4. Сохраните файл
git add .
git commit -m "Решил конфликт в документации"
git push
```

### Забыли добавить описание к коммиту
```bash
git commit --amend -m "Новое описание"
```

### Хотите отменить последний коммит (но сохранить изменения)
```bash
git reset --soft HEAD~1
```

### Случайно добавили ненужный файл
```bash
git reset имя-файла.md    # Убрать из staging
```

## 🤖 AI-помощник для Git

**Если команды не работают или непонятно что делать:**

1. **Позовите Claude/Copilot** в VS Code
2. **Опишите проблему:** "У меня ошибка в git, вот что показывает терминал: [скопируйте ошибку]"
3. **Следуйте инструкциям** AI - он поможет решить любую проблему

## 📊 Полезные команды для продвинутых

```bash
git log --oneline           # История коммитов кратко
git diff                    # Что изменилось в файлах
git branch                  # Показать текущую ветку
git remote -v               # Показать удаленные репозитории
```

## 🎯 Главное правило

**Не бойтесь экспериментировать!** Git сохраняет всю историю, поэтому:
- ❌ Невозможно потерять данные навсегда
- ✅ Всегда можно откатиться к предыдущей версии
- 🤝 AI-помощники всегда помогут решить проблемы

---

## 🚀 Быстрая шпаргалка

| Задача | Команда |
|--------|---------|
| Проверить статус | `git status` |
| Сохранить все изменения | `git add . && git commit -m "Описание"` |
| Отправить команде | `git push` |
| Получить обновления | `git pull` |
| Посмотреть историю | `git log --oneline` |

**Помните:** `git status` - ваш лучший друг! Используйте эту команду когда не уверены что происходит.

---

💡 **Совет:** Сохраните эту инструкцию в закладки и обращайтесь к ней при работе с документацией!